import 'package:flutter/material.dart';


// Example widget for BasicExample
class BasicExample extends StatelessWidget {
  const BasicExample({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: const EdgeInsets.all(16.0),
      color: Colors.greenAccent,
      child: const Text(
        'Basic Example',
        style: TextStyle(fontSize: 18, color: Colors.white),
      ),
    );
  }
}

// Example widget for CustomBackgroundExample
class CustomBackgroundExample extends StatelessWidget {
  const CustomBackgroundExample({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: const EdgeInsets.all(16.0),
      color: Colors.purpleAccent,
      child: const Text(
        'Custom Background Example',
        style: TextStyle(fontSize: 18, color: Colors.white),
      ),
    );
  }
}

// Example widget for ChangeTodyHighlightColorExample
class ChangeTodyHighlightColorExample extends StatelessWidget {
  const ChangeTodyHighlightColorExample({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: const EdgeInsets.all(16.0),
      color: Colors.redAccent,
      child: const Text(
        'Change Today Highlight Color Example',
        style: TextStyle(fontSize: 18, color: Colors.white),
      ),
    );
  }
}

// Example widget for ChangeDayStructureExample
class ChangeDayStructureExample extends StatelessWidget {
  const ChangeDayStructureExample({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: const EdgeInsets.all(16.0),
      color: Colors.tealAccent,
      child: const Text(
        'Change Day Structure Example',
        style: TextStyle(fontSize: 18, color: Colors.white),
      ),
    );
  }
}

// Example widget for LandscapeViewExample
class LandscapeViewExample extends StatelessWidget {
  const LandscapeViewExample({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: const EdgeInsets.all(16.0),
      color: Colors.yellowAccent,
      child: const Text(
        'Landscape View Example',
        style: TextStyle(fontSize: 18, color: Colors.white),
      ),
    );
  }
}

// Example widget for LocalSupportExample
class LocalSupportExample extends StatelessWidget {
  const LocalSupportExample({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: const EdgeInsets.all(16.0),
      color: Colors.cyanAccent,
      child: const Text(
        'Local Support Example',
        style: TextStyle(fontSize: 18, color: Colors.white),
      ),
    );
  }
}

// Example widget for ChangeHeaderAppearanceExample
class ChangeHeaderAppearanceExample extends StatelessWidget {
  const ChangeHeaderAppearanceExample({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: const EdgeInsets.all(16.0),
      color: Colors.indigoAccent,
      child: const Text(
        'Change Header Appearance Example',
        style: TextStyle(fontSize: 18, color: Colors.white),
      ),
    );
  }
}

// Example widget for EasyDateTimeLineItemBuilderExample
class EasyDateTimeLineItemBuilderExample extends StatelessWidget {
  const EasyDateTimeLineItemBuilderExample({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: const EdgeInsets.all(16.0),
      color: Colors.pinkAccent,
      child: const Text(
        'Easy Date Time Line Item Builder Example',
        style: TextStyle(fontSize: 18, color: Colors.white),
      ),
    );
  }
}
